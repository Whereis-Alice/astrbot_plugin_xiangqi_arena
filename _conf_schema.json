{
  "engine_backend": {
    "description": "AI 引擎模式",
    "type": "string",
    "default": "auto",
    "options": [
      "auto",
      "pikafish",
      "xqwlight",
      "builtin"
    ],
    "hint": "auto 会按 Pikafish -> xqwlight -> builtin 的顺序尝试；任一外部引擎失败后会进入冷却并自动降级。"
  },
  "show_engine_details": {
    "description": "回复中显示引擎细节",
    "type": "bool",
    "default": false,
    "hint": "关闭时玩家只看到走法和棋盘；开启时会显示引擎名、搜索时间和失败降级原因。"
  },
  "enable_pikafish_engine": {
    "description": "启用 Pikafish",
    "type": "bool",
    "default": true,
    "hint": "推荐 Linux 服务器使用。需要在 pikafish_path 配置 Pikafish 可执行文件，或把 pikafish 放进 PATH。"
  },
  "pikafish_path": {
    "description": "Pikafish 可执行文件路径",
    "type": "string",
    "default": "pikafish",
    "hint": "Linux 示例：/opt/pikafish/pikafish。默认值会从 PATH 中查找 pikafish。"
  },
  "pikafish_working_dir": {
    "description": "Pikafish 工作目录",
    "type": "string",
    "default": "",
    "hint": "通常留空。若引擎需要在特定目录读取 NNUE 文件，可填写 Pikafish 所在目录。"
  },
  "pikafish_eval_file": {
    "description": "Pikafish NNUE 文件",
    "type": "string",
    "default": "",
    "hint": "通常留空，让引擎使用默认设置。若需要指定 NNUE，可填写文件名或绝对路径。"
  },
  "pikafish_threads": {
    "description": "Pikafish 线程数",
    "type": "int",
    "default": 1,
    "hint": "低配服务器建议 1。线程越多越吃 CPU。"
  },
  "pikafish_hash_mb": {
    "description": "Pikafish Hash 内存 MB",
    "type": "int",
    "default": 16,
    "hint": "低配服务器建议 16 或 32。内存宽裕可调大。"
  },
  "pikafish_movetime_ms": {
    "description": "Pikafish 每步思考毫秒",
    "type": "int",
    "default": 500,
    "hint": "推荐用时间限制而不是固定深度。低配服务器建议 300-700。"
  },
  "pikafish_startup_timeout": {
    "description": "Pikafish 启动超时秒数",
    "type": "int",
    "default": 5,
    "hint": "启动 UCI 进程并等待 uciok/readyok 的超时时间。"
  },
  "pikafish_move_overhead_ms": {
    "description": "Pikafish Move Overhead 毫秒",
    "type": "int",
    "default": 30,
    "hint": "给引擎预留的通讯开销。低配服务器保持默认即可。"
  },
  "pikafish_failure_cooldown_seconds": {
    "description": "Pikafish 失败冷却秒数",
    "type": "int",
    "default": 600,
    "hint": "Pikafish 超时、崩溃或路径错误后，在这段时间内直接尝试下一个引擎。填 0 可关闭冷却。"
  },
  "enable_xqwlight_engine": {
    "description": "启用 xqwlight 兜底",
    "type": "bool",
    "default": true,
    "hint": "Pikafish 不可用时可继续尝试随包 xqwlight Java 引擎；低配服务器会用较低默认深度和失败冷却。"
  },
  "xqwlight_jar_path": {
    "description": "xqwlight jar 路径",
    "type": "string",
    "default": "",
    "hint": "留空使用插件内置 engine/bin/xqwlight-cli.jar。"
  },
  "xqwlight_depth": {
    "description": "xqwlight 搜索深度",
    "type": "int",
    "default": 4,
    "hint": "低配服务器建议 2-4。越大越强但更容易超时。"
  },
  "xqwlight_timeout_ms": {
    "description": "xqwlight 单步超时毫秒",
    "type": "int",
    "default": 800,
    "hint": "低配服务器建议 500-1000。超时会自动尝试 builtin，并进入失败冷却。"
  },
  "xqwlight_failure_cooldown_seconds": {
    "description": "xqwlight 失败冷却秒数",
    "type": "int",
    "default": 600,
    "hint": "xqwlight 超时或异常后，在这段时间内直接跳过 xqwlight，避免每步都等待 Java 超时。填 0 可关闭冷却。"
  },
  "ai_depth": {
    "description": "builtin 搜索层数",
    "type": "int",
    "default": 1,
    "hint": "仅 builtin 使用。低配服务器建议 1；2 更稳；3 会明显更慢。"
  },
  "llm_talk_enabled": {
    "description": "Bot 走棋后生成回复",
    "type": "bool",
    "default": true,
    "hint": "开启后，Bot 每次实际落子后会用当前 AstrBot 默认人格生成一句短回复；超时会使用本地兜底台词。"
  },
  "llm_provider_id": {
    "description": "回复模型 Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "留空使用当前会话/全局默认模型。"
  },
  "llm_model": {
    "description": "回复模型名",
    "type": "string",
    "default": "",
    "hint": "留空使用 Provider 默认模型。通常不需要填写。"
  },
  "llm_extra_prompt": {
    "description": "额外台词提示",
    "type": "text",
    "default": "",
    "hint": "默认使用 AstrBot 当前选择的人格。这里仅用于追加下棋口吻约束，不填也可以。"
  },
  "llm_talk_timeout": {
    "description": "回复生成超时秒数",
    "type": "int",
    "default": 3,
    "hint": "低配服务器建议 2-3。超时后会使用本地兜底台词，不影响棋局。"
  },
  "llm_talk_failure_cooldown_seconds": {
    "description": "回复失败冷却秒数",
    "type": "int",
    "default": 600,
    "hint": "回复模型超时或异常后，在这段时间内直接使用本地兜底台词，避免每步都等待模型超时。"
  },
  "llm_talk_max_sentences": {
    "description": "回复最多句数",
    "type": "int",
    "default": 1,
    "hint": "为避免刷屏，建议保持 1。最大有效值为 3。"
  },
  "llm_talk_max_chars": {
    "description": "单句最大字数",
    "type": "int",
    "default": 45,
    "hint": "用于防止模型输出太长。建议 30-60。"
  },
  "image_scale": {
    "description": "棋盘图片缩放",
    "type": "int",
    "default": 1,
    "hint": "1 为默认大小；2 为高清大图。"
  }
}
